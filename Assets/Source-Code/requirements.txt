# SQLInsight - Python dependencies
# scikit-learn is pinned EXACTLY to match the version the committed model
# artifacts (ml/artifacts/*.pkl/*.joblib) were trained with. This guarantees
# the pre-trained model loads cleanly on macOS without retraining.
scikit-learn==1.8.0
numpy>=2.0,<3
scipy>=1.13
joblib>=1.3
pandas>=2.0

# Web backend
Flask>=3.0,<4
requests>=2.30
python-dotenv>=1.0
